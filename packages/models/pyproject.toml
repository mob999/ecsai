[project]
name = "edge-sim-models"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = ["pydantic>=2.10,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/edge_sim_models"]
